const Logo = () => {
    return (
        <div>
            <span>deny</span> dalal
            <style jsx>{`
          div {
            font-weight: 700;
            opacity: 0.7;
            text-transform: uppercase;
            cursor: pointer;
          }
          span {
            color: #00a5ec
          }
        `}</style>
        </div>
    );
};

export default Logo;